# 博客笔记待确认公式错误清单

> 本清单记录审查 `source\_posts` 各科笔记（量子力学、量子力学2、电动力学等）时发现的**存疑但未直接修复**的内容。
> 分为两类：
> - **A 类（表述/推导存疑）**：中间步骤或表述有问题，但最终结果正确，需要作者确认后决定是否修改。
> - **B 类（定义缺失）**：用到了未定义的量，需要补充定义。
>
> 编号、位置（相对仓库根目录）、原文、问题、建议修改均列于下。确认前暂不修改源文件。

---

## 1. [B 类] Time-Dependent-Perturbation.md —— Rabi 振荡中的 `γ` 未定义

- **文件**：`source\_posts\量子力学2\Time-Dependent-Perturbation.md`
- **位置**：L76、L79-81
- **原文**：
  - L76：`$$\Omega=\sqrt{\frac{V^2}{\hbar^2}+\frac{(\omega+\omega_{12})^2}{4}}$$`
  - L79：`$$c_2(t)=-\frac{\gamma}{2\Omega\hbar}e^{i\Omega t}e^{i\frac{\omega+\omega_{12}}{2}t}+\frac{\gamma}{2\Omega\hbar}e^{-i\Omega t}e^{i\frac{\omega+\omega_{12}}{2}t}=-\frac{i\gamma}{\Omega\hbar}\sin{[\Omega t]}e^{i\frac{\omega+\omega_{12}}{2}t}$$`
  - L80：`$$|c_2(t)|^2=\frac{\gamma^2}{\hbar^2\Omega^2}\sin^2{[\Omega t]}$$`
- **问题**：`Ω` 已定义，但振荡解中的振幅参数 `γ` 全文未定义。
- **判断**：从解的结构看 `γ` 应为耦合强度 `V`，即 `c_2(t)=-\frac{iV}{\hbar\Omega}\sin[\Omega t]e^{i\frac{\omega+\omega_{12}}{2}t}`。
- **建议**：将 L79-81 中的 `γ` 改为 `V`（并核对 `γ` 是否在别处被定义过，若没有则确认替换）。

---

## 2. [A 类] Density-Matrix.md —— `Tr(ρ²)` 的说明与展开式有误

- **文件**：`source\_posts\量子力学2\Density-Matrix.md`
- **位置**：L137、L144
- **原文**：
  - L137：`> 切不可认为$Tr(\hat\rho^2)=\sum_iP_i^2$！这是因为这样描述下的$\hat\rho^2$是在非正交纯态基上描述的，那么trace就不是一个良定义的量（不再是相似不变量）。`
  - L144：`> $$Tr(\hat \rho^2)=\sum_n\sum_m(\sum_{i}\sum_{j} p_ip_j|c_{in}|^2|c_{jm}|^2)<1$$`
- **问题**：
  1. L137 的解释错误：**迹是相似不变量，与基的选取无关，在任何基下都是良定义的**。正确的原因应该是：`Tr(ρ²)=Σ_i Σ_j P_iP_j|⟨ψ_i|ψ_j⟩|²`（见 L130-134 的严格推导），仅当纯态系综 `{|ψ_i⟩}` 彼此正交时才退化为 `Σ_iP_i²`，所以不能说"trace 不是良定义的量"。
  2. L144 的展开式不是基无关的：`Tr(ρ²)=Σ_n Σ_m (Σ_i Σ_j p_ip_j|c_in|²|c_jm|²)` 的写法与迹的定义不符（缺少 `⟨e_n|e_m⟩` 的交叉项结构），并且即使对单个纯态也会得到 `<1` 而非 `1`，与 L130-134 的严格结果 `Tr(ρ²)=Σ_ijP_iP_j|⟨ψ_i|ψ_j⟩|²` 不一致。
- **建议**：修改 L137 的说明文字（迹始终是良定义的相似不变量，问题在于 `ΣP_i²` 仅在纯态系综正交时才等于迹）；修改或删除 L144 的展开式，改用 L130-134 的严格式。

---

## 3. [A 类] Scattering-Theory.md —— 光学定理证明的符号链需核对

- **文件**：`source\_posts\量子力学2\Scattering-Theory.md`
- **位置**：L144-161
- **原文**：
  - L145：`$$\frac{k}{4\pi}\sigma_{tot}=\Im [f(\theta=0,\phi=0)]$$`（光学定理陈述，形式正确）
  - 证明链条（节选）：
    - L148：`\Im{f(\theta=0)}=\frac{4\pi^2m}{\hbar^2}\Im{\langle \vec{k_i}|\hat T^+|\vec{k_i}\rangle}`
    - L149：`=\frac{4\pi^2m}{\hbar^2}\Im{\langle \psi_k|(1-\hat V\hat G_0^-)\hat V|\psi_k\rangle}`
    - L153：`=\frac{4\pi^3m}{\hbar^2}\langle \psi_k|\hat V\delta(E-H_0)\hat V|\psi_k\rangle`
    - L157-160：`=\frac{4\pi^3m^2}{\hbar^4}k\int_{|k|=|k_i|}|\langle \vec{k}|\hat T|\vec{k}_i\rangle|^2 d\Omega=\frac{k}{4\pi}\int d\Omega|f(\theta,\phi)|^2=\frac{k}{4\pi}\sigma_{tot}`
- **问题**：
  1. 证明中隐含使用了散射振幅与 T 矩阵的归一化关系 `f = (4π²m/ℏ²)⟨k'|T|k⟩`。标准关系是 `f = -(m/2πℏ²)⟨k'|T|k⟩`，两者相差 `8π³` 的系数和符号。需要确认笔记中 T 矩阵与 f 的定义（见本文件前面的 Born 展开 / 散射振幅小节）与证明链条是否自洽。
  2. L149 的 `T=(1-\hat V\hat G_0^-)\hat V` 以及 L150 丢弃 `(1-·)` 中其余部分、取 `Im` 的处理需要核对（标准做法是从 `T=V+VG_0^+T` 或 `T=V+TG_0^-V` 出发）。
  3. L153 的 δ 函数系数 `4π³m/ℏ²`（由 `1/(E-H_0-iε)` 取虚部得到 `∓πδ`）需确认与 L148 的系数一致。
- **判断**：结尾 `(k/4π)σ_tot` 与开头一致，链条内部自洽；主要疑点是 T 矩阵归一化约定与教材/前文是否一致。
- **建议**：核对 L148 中 `f` 与 `T` 的换算系数，统一为笔记前文使用的约定（或改为标准式 `f=-(m/2πℏ²)⟨k'|T|k⟩` 并同步调整整个系数链）。

---

## 4. [A 类] Basic-Concept.md —— "测量"小节表述令人困惑

- **文件**：`source\_posts\量子力学2\Basic-Concept.md`
- **位置**：L162-165
- **原文**：
  - L163：`当我们用算符作用在一个右矢上，其物理意义是测量:`
  - L164：`$$|\alpha\rangle=\hat A|a\rangle$$`
  - L165：`测量一个右矢，给出了相应算符表象下物理态及其概率。`
- **问题**：
  1. 将"算符作用在右矢上"等同于"测量"是误导性的：测量应描述为投影算符作用与本征态坍缩，而不是一般算符（如 `Â`）的任意作用。
  2. L164 中 `α` 与 `a` 未定义（`a` 是本征值还是本征态指标？`|a⟩` 是否为本征右矢？），等式本身也不符合测量公设。
- **建议**：改写为投影算符表述，例如 `\hat P_a|\psi\rangle=|\langle a|\psi\rangle|^2|\psi_a\rangle` 或测量后态 `|\psi\rangle\to|\psi_a\rangle`；并明确 `α`、`a` 的含义（或删除该例）。

---

## 5. [A 类] 中心力场问题.md —— 氢原子磁矩推导的中间步骤不成立

- **文件**：`source\_posts\量子力学\中心力场问题.md`
- **位置**：L122
- **原文**：
  - `$$\begin{aligned}M_z&=\int SdI\\&=-e\int \pi(r\sin\theta)^2\frac{m_l\hbar}{m}\frac{1}{r\sin\theta}|\psi|^2d\sigma\\&=\frac{-em_l\hbar}{2m}\int 2\pi r\sin\theta |\psi|^2d\sigma\\&=\frac{-em_l\hbar}{2m}\int|\psi|^2d\tau\\&=\frac{-em_l\hbar}{2m}\\&=-\mu_B m_l\end{aligned}$$`
- **问题**：中间一步 `\int 2\pi r\sin\theta|\psi|^2 d\sigma=\int|\psi|^2 d\tau` 不成立：取 `d\sigma=r^2\sin\theta\,d\theta d\varphi` 时，左端 `=\int 2\pi r^3\sin^2\theta|\psi|^2d\theta d\varphi`，而右端 `=\int|\psi|^2r^2\sin\theta\,dr d\theta d\varphi`，二者积分结果一般不等。
- **判断**：最终结果 `M_z=-m_l\mu_B` 正确，问题仅在推导的中间步骤。
- **建议**：改用标准推导：`M_z=-\frac{e}{2}\int(\vec r\times\vec j)_z\,dV=-\frac{e}{2}\int r\sin\theta\,j_\varphi\,d\tau=-\frac{em_l\hbar}{2m}\int|\psi|^2d\tau=-\mu_B m_l`。

---

## 6. [A 类] 电磁波.md —— 矩形波导最低 TE 模的陈述依赖尺寸约定

- **文件**：`source\_posts\电动力学\电磁波.md`
- **位置**：L84
- **原文**：`所以$\omega_{mn}$是TE波在$TE_{mn}$模式的截止频率。最低的模式为$TE_{01}$，因为如果$m=n=0$，则$B_z=Const$，与$\nabla\times \vec{E}=0$相悖。`
- **问题**：截止频率 `ω_mn=c√((mπ/a)²+(nπ/b)²)`，最低模取决于 a、b 的相对大小：若 `a≥b` 则最低模为 `TE_10`，若 `b≥a` 则为 `TE_01`。文中未声明尺寸约定，直接断言 `TE_01` 最低，在通常 `a≥b` 的约定下不成立。
- **判断**：`m=n=0` 时 `B_z=Const` 与法拉第定律相悖的论证正确（该模不存在）；但"最低模式"的结论取决于 a、b 的相对大小。
- **建议**：改为"最低模式为 `TE_10`（约定 `a≥b`）"或按本笔记的尺寸约定补充说明。

---

*记录时间：2026-08-11。待逐项确认后再决定是否修改源文件。*
